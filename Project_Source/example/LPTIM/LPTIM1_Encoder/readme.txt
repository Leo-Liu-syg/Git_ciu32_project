本示例使用到的资源:
LPTIM1的IN1输入  --   PB5
LPTIM1的IN2输入  --   PB7


本示例的功能流程:
1、上电配置系统时钟PLL;
2、配置LPTIM1:初始化LPTIM1(工作时钟内部(RCL)，计数时钟外部，软件触发)；
3、使能LPTIM1的自动重载匹配中断、传输方向改变中断，使能LPTIM1，启动正交编码；
3、当LPTIM1正交编码计数到周期值后产生自动重载匹配中断；
4、计数过程中出现计数方向转换后产生方向改变中断。

注意：
1、IN1、IN2引脚上输入正交编码器波形。