本示例使用到的资源：
TAMP_IN  --   PC13

本示例的功能流程：
1、上电配置时钟（PLL 48MHz）；
2、初始化TAMP配置：
   使能并开启RCL，设置TAMP时钟为RCL；
   使能外部入侵TAMP_IN管脚；
3、配置TAMP_IN外部入侵：
   配置下降沿触发，使能引脚上拉电阻；
   备份寄存器写入特征值，使能入侵检测擦除备份寄存器；
   使能TAMP；
   配置并使能TAMP中断；
4、按下按键产生TAMP_IN信号，触发中断；
5、确认备份寄存器被清除。
