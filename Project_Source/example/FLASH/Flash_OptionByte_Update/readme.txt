本示例使用到的资源：
无

本示例的功能流程：
1、上电配置时钟（PLL 48MHz）：
2、USER按键初始化；
3、等待用户按下USER按键；
4、Flash选项字节解锁；
5、选项字节擦除；
6、选项字节OPTR2参数RST_STOP与IWDG_STOP分别赋值默认值相反值0，进行选项字节编程与校验；
7、执行选项字节加载，产生复位；
8、下一次按键后，选项字节编程默认值，重复以上：
   选项字节OPTR2参数RST_STOP与IWDG_STOP分别赋值默认值1，进行选项字节编程与校验。