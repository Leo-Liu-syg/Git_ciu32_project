本示例使用到的资源：
MCO          --   PA8

本示例的功能流程：
1、上电默认系统时钟RCHSYS 16MHz；
2、配置MCO输出系统时钟，即16MHz；
3、配置TIM3 通道4输入捕获源为LXTAL，作为校准RCH时钟源；
4、通过LXTAL时钟和RCH的校准寄存器可实现动态校准RCH，以获取全温度范围内更高精度、更高可靠性的RCH时钟。
5、在使用LXTAL的应用中，推荐在使用RCH时钟前（如上电或stop低功耗唤醒后）或通过RTC定时，周期性对RCH校准，校准后的RCH时钟精度可达0.4%。
