本示例使用到的资源：
LED1       --   PB14

本示例功能流程：
1、上电配置系统时钟（PLL、48M）、初始化LED1；
2、初始化WWDG：配置窗口为0x20、计数值为0x0A、预分频为128分频，喂狗后到进入窗口所需的时间约为251.2ms；
3、每隔（251.2+5）ms执行一次喂狗操作，同时翻转LED1，WWDG不会产生复位。

注意：
1、调试模式下为防止复位，建议程序全速运行。