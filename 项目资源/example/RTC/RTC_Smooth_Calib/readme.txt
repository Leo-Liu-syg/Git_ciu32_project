本示例使用到的资源：
RTC_OUT引脚   --   PC13

本示例的功能流程：
1、上电配置时钟（PLL 48MHz）；
2、初始化RTC配置：
   使能并开启LXTAL，设置RTC时钟为LXTAL晶体模式；
3、配置RTC_OUT输出SPRE 1Hz，可使用频率计测量此时输出频率与1Hz的偏差，修改FRTC_PPM的值；
4、等待3秒；
5、根据输入的当前LXTAL偏差ppm值，计算校准参数；
6、使能校准周期中断，并配置数字平滑校准寄存器；
   校准参数写入后，平滑校准立刻生效；
   写入时可能在校准周期32s的中间，故一般在第二次校准周期中断后的测量最准确。
